#!/bin/sh
docker stop santa-bot
docker rm santa-bot

docker build -t santa-bot .
docker run -d \
    --restart=on-failure:3 \
    -v /home/astea/santa-bot/data:/santa-bot/data \
    -v /home/astea/santa-bot/log:/santa-bot/log \
    --name santa-bot \
    santa-bot
